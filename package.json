{
  "name": "telecom-networks-crf",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "headers": "node scripts/generate-headers.mjs",
    "headers:check": "node scripts/generate-headers.mjs --check",
    "verify": "npm run headers:check && npm run test && npm run build && npm run test:e2e"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@vitejs/plugin-react": "^4.3.4",
    "jszip": "^3.10.2",
    "vite": "^6.0.5",
    "vitest": "^5.0.0"
  },
  "overrides": {
    "uuid": "^11.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
